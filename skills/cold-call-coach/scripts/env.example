# Copy to .env in your project folder (next to the scripts folder).
# OpenRouter API key for transcription (openrouter.ai -> Keys). Cost is about
# $0.002 per audio minute with google/gemini-2.5-flash.
OPENROUTER_API_KEY=sk-or-...
